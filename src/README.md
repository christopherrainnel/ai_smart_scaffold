# Source

`src/` is the standard home for functional application logic in a software project.

AI Smart Scaffold keeps this folder visible as part of the baseline scaffold it promotes for building maintainable projects with AI assistance.

Public release binaries are normally distributed through packaged release assets rather than committed into this folder by default.
