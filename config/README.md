# Config

`config/` is the standard location for shared application settings, templates, and non-secret environment references.

Secrets should not be committed to this repository. Public repositories should store only safe defaults, examples, or documented configuration contracts.
