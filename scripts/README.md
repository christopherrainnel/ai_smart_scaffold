# Scripts

`scripts/` is the standard location for automation used in build, packaging, validation, and release workflows.

This folder is part of the baseline project structure AI Smart Scaffold recommends for keeping repeatable operational tasks explicit and maintainable.
