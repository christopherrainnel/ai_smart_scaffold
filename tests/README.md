# Tests

`tests/` is the standard location for validation and regression coverage.

Keeping tests separate from `src/` helps production builds stay lean while preserving a clear place for quality checks, evaluation harnesses, and release verification.
